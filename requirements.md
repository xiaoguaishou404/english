# 3000 词网站功能需求（首版）

## 1. 目标与范围
- 面向自学用户，围绕 `english.txt` 的 3000 高频词内容提供分批学习体验。
- 首版必须包含：分批浏览、学习进度、单词备注。
- 不做账号体系与云同步，默认单机本地存储。

## 2. 用户与场景
- 用户类型：个人自学者。
- 典型场景：按批次浏览 → 标记已掌握 → 对单词加备注 → 查看整体进度。

## 3. 信息架构与页面清单
1. 首页
   - 入口卡片：继续上次批次、总进度、最近备注。
2. 批次列表页
   - 展示 1–12 批，显示每批词数、已掌握数量、完成率。
3. 批次详情页
   - 批次信息（范围、标签）
   - 词表列表（可筛选：全部/未掌握/已掌握）
   - 右侧或抽屉：单词详情与备注
4. 进度概览页
   - 总进度、批次进度、最近学习记录
5. 设置页（可选简化）
   - 清空进度、导入/导出（可留作后续）

## 4. 核心功能需求
### 4.1 分批浏览
- 将 `english.txt` 中的 12 批词表解析为批次数据。
- 支持从批次列表进入批次详情。
- 批次详情内支持列表滚动与快速定位。

### 4.2 学习进度
- 每个单词可标记为：未掌握/已掌握。
- 批次与全局完成率自动计算。
- 支持“只看未掌握/已掌握/全部”的简单筛选。

### 4.3 单词备注
- 每个单词支持一段文本备注（可空）。
- 备注在单词详情区编辑并自动保存。
- 支持按“有备注”筛选（可选，优先级中等）。

### 4.4 基础交互与状态
- 默认记住上次访问的批次与筛选状态。
- 点击单词时，在详情区展示词条、批次、进度与备注。
- 列表项展示：单词 + 进度标识 + 是否有备注。

## 5. 业务规则与边界
- 词表来源以 `english.txt` 为准，解析错误或缺失批次时需提示。
- 进度与备注仅保存在本地，不做跨设备同步。
- 不强制词性、发音、释义；后续可扩展字段。

## 6. 数据模型（建议）
### 6.1 Word
- `id`: string（批次+序号的组合）
- `text`: string
- `batchId`: string（例如 "batch-01"）
- `tags`: string[]（可选，预留）

### 6.2 Batch
- `id`: string
- `title`: string（例如 "第 1 批"）
- `range`: string（例如 "Top 1–250"）
- `words`: Word[]

### 6.3 Progress
- `wordId`: string
- `status`: "unknown" | "known"
- `updatedAt`: number（时间戳）

### 6.4 Note
- `wordId`: string
- `content`: string
- `updatedAt`: number

## 7. 状态与存储策略
- 本地存储优先方案：
  - 小规模数据：`localStorage`
  - 若后续扩展：`IndexedDB` 作为升级选项
- 推荐存储结构：
  - `progressByWordId`: Record<string, Progress>
  - `notesByWordId`: Record<string, Note>
  - `lastVisited`: { batchId, filter }

## 8. 非功能需求
- 性能：批次详情 250 词内流畅滚动。
- 可访问性：键盘可操作、对比度达标、焦点可见。
- 兼容性：现代浏览器（Chrome/Edge/Firefox/Safari）最新两个版本。
- 国际化：界面文案集中管理，预留中英切换能力。

## 9. 后续里程碑建议
- M1：完成分批浏览 + 本地进度 + 备注（首版 MVP）
- M2：增加导出 TXT/Markdown/CSV 与筛选增强
- M3：加入测验/复习模式（随机/间隔复习）
